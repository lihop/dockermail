machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t dovecot dovecot/
test:
  override:
    # test postfix
    - docker run -d --name imap-server -p 0.0.0.0:25:25 -p 0.0.0.0:587:587 -p 0.0.0.0:143:143 dovecot:2.1.7